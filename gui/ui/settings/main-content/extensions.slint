// Extensions settings category page for the Operon GUI.
import { Tokens } from "../../shared/tokens.slint";

export component ExtensionsSettings inherits Rectangle {
    background: transparent;
    
    VerticalLayout {
        padding: 24px;
        spacing: 16px;
        alignment: start;
        
        HorizontalLayout {
            spacing: 12px;
            alignment: start;
            
            Image {
                source: @image-url("../../../assets/icons/settings/category-extensions.svg");
                width: 20px;
                height: 20px;
                colorize: Tokens.text_primary;
            }
            
            Text {
                text: "Extensions Settings";
                color: Tokens.text_primary;
                font-size: Tokens.body_font_size + 4px;
                font-weight: 600;
            }
        }
        
        Text {
            text: "Browse, update, and manage installed plugins and IDE extensions.";
            color: Tokens.text_muted;
            font-size: Tokens.body_font_size;
            wrap: TextWrap.word-wrap;
        }
    }
}

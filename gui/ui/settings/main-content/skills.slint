// Skills settings category page for the Operon GUI.
import { Tokens } from "../../shared/tokens.slint";

export component SkillsSettings inherits Rectangle {
    background: transparent;
    
    VerticalLayout {
        padding: 24px;
        spacing: 16px;
        alignment: start;
        
        HorizontalLayout {
            spacing: 12px;
            alignment: start;
            
            Image {
                source: @image-url("../../../assets/icons/settings/category-skills.svg");
                width: 20px;
                height: 20px;
                colorize: Tokens.text_primary;
            }
            
            Text {
                text: "Skills Settings";
                color: Tokens.text_primary;
                font-size: Tokens.body_font_size + 4px;
                font-weight: 600;
            }
        }
        
        Text {
            text: "Browse, install, and configure custom agent skills and automated tools.";
            color: Tokens.text_muted;
            font-size: Tokens.body_font_size;
            wrap: TextWrap.word-wrap;
        }
    }
}

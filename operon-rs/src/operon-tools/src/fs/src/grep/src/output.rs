// Output for the grep tool is plain text (ToolContent::Text). No struct needed.
//
// The previous GrepOutput, FileGrepResult, and GrepLine structs have been removed.
// All formatting happens directly in executor.rs and is returned as a String.

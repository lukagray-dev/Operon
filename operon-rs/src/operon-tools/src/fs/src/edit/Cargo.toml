[package]
name        = "operon-tools-fs-edit"
description = "Edit tool: exact-string multi-hunk file editing with atomic writes. Supports one or more old_string→new_string replacements per call, applied in order, committed in a single atomic disk write."
version.workspace    = true
edition.workspace    = true
authors.workspace    = true
license.workspace    = true
repository.workspace = true

[dependencies]
thiserror.workspace                      = true
serde_json.workspace                     = true
tokio.workspace                          = true
operon-context-normalize.workspace = true
operon-tools-core.workspace              = true

[dev-dependencies]
tempfile.workspace = true

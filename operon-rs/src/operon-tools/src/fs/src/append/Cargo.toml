[package]
name        = "operon-tools-fs-append"
description = "Append tool: appends text to the end of an existing file. Non-destructive — never modifies existing content. File must already exist; use write to create new files."
version.workspace    = true
edition.workspace    = true
authors.workspace    = true
license.workspace    = true
repository.workspace = true

[dependencies]
thiserror.workspace                      = true
serde.workspace                          = true
serde_json.workspace                     = true
tokio.workspace                          = true
operon-context-normalize-tools.workspace = true
operon-tools-core.workspace              = true

[dev-dependencies]
tempfile.workspace = true

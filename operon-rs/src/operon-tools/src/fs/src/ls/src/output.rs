// Output for the ls tool is plain text (ToolContent::Text). No struct needed.
//
// The previous LsOutput, LsEntry, and EntryKind structs have been removed.
// All formatting happens directly in executor.rs and is returned as a String.

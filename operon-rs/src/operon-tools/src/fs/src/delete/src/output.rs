// Output for the delete tool is plain text (ToolContent::Text). No struct needed.
//
// The previous DeleteOutput and DeletedKind structs have been removed.
// All output is formatted directly in executor.rs as a plain string.

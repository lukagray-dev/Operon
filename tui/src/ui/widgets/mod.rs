// Widgets module
// Stateless, reusable UI primitives
// No screen dependencies — these can be used anywhere

// Allow dead code for widgets that will be used as features are implemented
#[allow(dead_code)]

pub mod confirm_dialog;
pub mod notification;
pub mod spinner;

//! Settings Window Commands for Bridge.

/// Opens the Settings window/tab (dispatched by extension host).
pub async fn open_settings_window() -> Result<(), String> {
    Ok(())
}

/// Closes the Settings window/tab (dispatched by extension host).
pub async fn close_settings_window() -> Result<(), String> {
    Ok(())
}
